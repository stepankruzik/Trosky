// Zničit pneumatiku až poté, co někdo projel checkpointem 1
/*
if (instance_exists(obj_car) && obj_car.current_checkpoint >= 1) {
    // Nepovinně: můžeš přidat i AI podmínku, nebo ne
    instance_destroy(); // nebo třeba visible = false, solid = false;
}
if (instance_exists(obj_ai_car) && obj_ai_car.current_checkpoint >= 1) {
    // Nepovinně: můžeš přidat i AI podmínku, nebo ne
    instance_destroy(); // nebo třeba visible = false, solid = false;
}

if (instance_exists(obj_car_2) && obj_car_2.current_checkpoint >= 1) {
    // Nepovinně: můžeš přidat i AI podmínku, nebo ne
    instance_destroy(); // nebo třeba visible = false, solid = false;
}
*/

